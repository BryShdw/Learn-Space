# Environment variables for Learn Space application

# Database Configuration
DB_HOST=localhost
DB_USER=your_mysql_user
DB_PASSWORD=your_mysql_password
DB_DATABASE=learn_space_db # Updated to match the new setup script

# Express Session
SESSION_SECRET=your_very_strong_session_secret_key_here # Change this to a random, strong string

# Application Port (Optional, defaults to 3000 if not set in app.js)
# PORT=3000
